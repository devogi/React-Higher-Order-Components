import React, { Component } from 'react'

const layout = (Component) => ({...props}) => (
    <div>
        <AppBar />
        <SideBar />
        <div>
            
        </div>
    </div>
)

export default HocIndex