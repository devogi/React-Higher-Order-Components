import React, { Component } from 'react'

const layout = (Component) => ({...props}) => (
    <div>
        <AppBar />
        <SideBar />
        <div style={{ position: }} >

        </div>
    </div>
)

export default HocIndex