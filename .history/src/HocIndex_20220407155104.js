import React, { Component } from 'react'

const layout = (Component) => ({...props}) => (
    <div>
        <AppBar />
        <SideBar />
        <div style={} >

        </div>
    </div>
)

export default HocIndex